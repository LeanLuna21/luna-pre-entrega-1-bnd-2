<nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img src="/img/icons/hamburguesa.png" alt="FastFeed-logo" width="30"> <span
                class="d-blcok ms-3">FastFeed</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 align-items-center">
                <li class="nav-item">
                    <a id="home" class="nav-link active" aria-current="page" href="/">Home</a>
                </li>
                {{#if user}}
                <li class="nav-item">
                    <a id="historial-pedidos" class="nav-link disabled" href="/profile" tabindex="-1"
                        aria-disabled="true">Mi perfil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout" tabindex="-1">LogOut</a>
                </li>

                <a class="btn btn-warning m-2" href="#start-order"> ARMAR PEDIDO <i
                        class="fas fa-chevron-right ms-2"></i></a>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link" href="/login" tabindex="-1">LogIn</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="/register" tabindex="-1">SignUp</a>
                </li>
                {{/if}}
            </ul>
            
            <div class="d-flex flex-row align-items-center justify-content-end">
                <span class="navbar-text text-muted d-none d-lg-inline me-3">
                    Fast-food, <b>faster</b>.
                </span>
                
                {{#if user}}
                <span class="navbar-text text-muted  me-3 ">
                    Bievenido, <b>{{ user.first_name }}</b>.
                </span>
                {{/if}}
                
                {{#if message}}
                <span class="navbar-text text-muted  me-3 ">
                    <b>{{ message }}</b>.
                </span>
                {{/if}}

                <button id="color-mode" class="mx-3"><i id="color-mode-icon"
                        class="fa-regular fa-lightbulb"></i></button>

            </div>
        </div>
    </div>


</nav>